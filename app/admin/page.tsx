'use client';

import { useEffect, useMemo, useState } from 'react';
import { api } from '@/lib/api';

type AdminData={restaurant:any;role:string;orders:any[];products:any[]};
const money=(value:number)=>Number(value||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});

export default function AdminPage(){
 const [tenant,setTenant]=useState('sabor-da-casa'); const [token,setToken]=useState(''); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [data,setData]=useState<AdminData|null>(null); const [error,setError]=useState(''); const [tab,setTab]=useState('Visão geral');
 useEffect(()=>{setTenant(new URLSearchParams(location.search).get('restaurant')||'sabor-da-casa');setToken(localStorage.getItem('mesaflow-token')||'');},[]);
 useEffect(()=>{if(token)load();},[token,tenant]);
 async function load(){try{setData(await api(`admin?restaurant=${tenant}`,{headers:{authorization:`Bearer ${token}`}}));setError('');}catch(e:any){setError(e.message);setData(null)}}
 async function login(){try{const result=await api('login',{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('mesaflow-token',result.access_token);setToken(result.access_token);}catch(e:any){setError(e.message)}}
 if(!token||!data)return <main className="admin-login"><section><span className="eyebrow">MESAFLOW · RESTAURANT OS</span><h1>Operação simples. Marca forte.</h1><p>Pedidos, cardápio e identidade da sua loja em um só lugar.</p></section><form onSubmit={e=>{e.preventDefault();login()}}><div className="admin-mark">M</div><h2>Bem-vindo de volta</h2><p>Entre para acessar seu restaurante.</p><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Senha<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="button button-dark" type="submit">Entrar no painel</button>{error?<small className="form-error">{error}</small>:null}</form></main>;
 const orders=data.orders||[]; const revenue=useMemo(()=>orders.filter(o=>o.status!=='cancelado').reduce((s,o)=>s+Number(o.total),0),[orders]); const tabs=['Visão geral','Pedidos','Cardápio','Aparência','Configurações'];
 return <div className="admin-shell"><aside className="admin-sidebar"><div className="admin-brand"><span className="admin-mark">M</span><div><b>MesaFlow</b><small>Restaurant OS</small></div></div><div className="tenant-chip"><span>{data.restaurant.name?.[0]}</span><div><b>{data.restaurant.name}</b><small>Plano Pro</small></div></div><nav>{tabs.map(item=><button className={tab===item?'selected':''} key={item} onClick={()=>setTab(item)}>{item}</button>)}</nav><div className="sidebar-bottom"><a href={`/loja/${tenant}`} target="_blank">↗ Ver minha loja</a><button onClick={()=>{localStorage.removeItem('mesaflow-token');setToken('');setData(null)}}>Sair</button></div></aside><main className="admin-content"><header><div><h1>{tab}</h1><p>{tab==='Visão geral'?'Acompanhe o que está acontecendo na operação.':'Gerencie sua loja com rapidez.'}</p></div><div className="admin-avatar">{data.restaurant.name?.[0]}</div></header>{tab==='Visão geral'?<><section className="metric-grid"><Metric label="Faturamento" value={money(revenue)} detail="pedidos registrados"/><Metric label="Pedidos" value={String(orders.length)} detail="total no período"/><Metric label="Em preparo" value={String(orders.filter(o=>o.status==='preparando').length)} detail="exigem atenção"/><Metric label="Concluídos" value={String(orders.filter(o=>o.status==='concluido').length)} detail="pedidos finalizados"/></section><Orders orders={orders}/></>:null}{tab==='Pedidos'?<Orders orders={orders}/>:null}{tab==='Cardápio'?<section className="admin-panel"><div className="panel-title"><div><h2>Cardápio</h2><p>Produtos cadastrados nesta loja.</p></div><button className="button button-dark">+ Novo produto</button></div><div className="menu-list">{data.products.map(p=><div className="menu-item" key={p.id}><span>🍴</span><div><b>{p.name}</b><small>{p.available?'Disponível':'Indisponível'}</small></div><strong>{money(p.price)}</strong><button>Editar</button></div>)}</div></section>:null}{tab==='Aparência'?<BrandEditor restaurant={data.restaurant} token={token} tenant={tenant} reload={load}/>:null}{tab==='Configurações'?<section className="admin-panel empty-panel"><h2>Configurações da loja</h2><p>Horários, entrega, equipe e cobrança entram nesta área.</p></section>:null}</main></div>
}
function Metric({label,value,detail}:{label:string,value:string,detail:string}){return <article className="metric-card"><span>{label}</span><strong>{value}</strong><small>{detail}</small></article>}
function Orders({orders}:{orders:any[]}){return <section className="admin-panel"><div className="panel-title"><div><h2>Pedidos recentes</h2><p>Últimos pedidos recebidos pela loja.</p></div></div><div className="order-table"><div className="order-row order-head"><span>Pedido</span><span>Cliente</span><span>Status</span><span>Total</span></div>{orders.slice(0,12).map(o=><div className="order-row" key={o.id}><b>#{o.public_number}</b><span>{o.customer_name}</span><span><i className="status-pill">{String(o.status).replaceAll('_',' ')}</i></span><strong>{money(o.total)}</strong></div>)}{orders.length===0?<div className="empty-row">Nenhum pedido recebido ainda.</div>:null}</div></section>}
function BrandEditor({restaurant,token,tenant,reload}:{restaurant:any;token:string;tenant:string;reload:()=>void}){const [form,setForm]=useState({...restaurant});const [saving,setSaving]=useState(false);async function save(){setSaving(true);try{await api(`branding?restaurant=${tenant}`,{method:'PATCH',headers:{authorization:`Bearer ${token}`},body:JSON.stringify(form)});await reload();}finally{setSaving(false)}}return <div className="brand-layout"><section className="admin-panel"><div className="panel-title"><div><h2>Identidade da marca</h2><p>Personalize como os clientes enxergam sua loja.</p></div></div><div className="brand-form"><label>Nome da loja<input value={form.name||''} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Slogan<input value={form.tagline||''} onChange={e=>setForm({...form,tagline:e.target.value})}/></label><label>Cor principal<input type="color" value={form.primary_color||'#111111'} onChange={e=>setForm({...form,primary_color:e.target.value})}/></label><label>Cor secundária<input type="color" value={form.secondary_color||'#555555'} onChange={e=>setForm({...form,secondary_color:e.target.value})}/></label></div><button className="button button-dark" onClick={save}>{saving?'Salvando…':'Salvar alterações'}</button></section><section className="brand-preview" style={{'--brand':form.primary_color,'--brand2':form.secondary_color} as React.CSSProperties}><span>PREVIEW DA LOJA</span><div><small>● ABERTO AGORA</small><h2>{form.name||'Sua loja'}</h2><p>{form.tagline||'Seu slogan aparece aqui'}</p><button>Fazer pedido</button></div></section></div>}
